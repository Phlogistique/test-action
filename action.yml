name: "Hello World"
description: "Greet someone and record the time"
inputs:
  who-to-greet:
    description: "Who to greet"
    required: true
    default: "World"
  GITHUB_TOKEN:
    description: "Github token"
    required: true
runs:
  using: "node12"
  main: "index.js"
branding:
  icon: "fast-forward"
  color: "green"
